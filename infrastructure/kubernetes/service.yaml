apiVersion: v1
kind: Service
metadata:
  name: nim-rag-agent
  namespace: nim-rag-agent
  labels:
    app: nim-rag-agent
spec:
  type: LoadBalancer
  ports:
  - port: 80
    targetPort: 8000
    protocol: TCP
    name: http
  selector:
    app: nim-rag-agent
