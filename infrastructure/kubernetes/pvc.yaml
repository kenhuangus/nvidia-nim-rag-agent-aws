apiVersion: v1
kind: PersistentVolumeClaim
metadata:
  name: chroma-data
  namespace: nim-rag-agent
spec:
  accessModes:
    - ReadWriteOnce
  resources:
    requests:
      storage: 10Gi
  storageClassName: gp2  # AWS EBS gp2 storage class
